# Utilities Environment Variables
# Copy this file to .env and fill in your actual values

# Database Configuration for Python utilities
DB_HOST=localhost
DB_NAME=loot_tracking
DB_USER=loot_user
DB_PASSWORD=your_database_password_here
DB_PORT=5432

# Usage:
# 1. Copy this file to .env in the utilities directory
# 2. Fill in your actual database password
# 3. Source the environment: source .env (Linux/Mac) or set variables in Windows
# 4. Run the Python scripts: python db_compare.py --master-port 5433

# Security Notes:
# - Never commit the actual .env file to version control
# - Use the same database credentials as your backend application
# - These scripts should only be run in development/maintenance scenarios
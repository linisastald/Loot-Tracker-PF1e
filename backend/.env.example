# Backend Environment Variables
# Copy this file to .env and fill in your actual values

# Database Configuration
DB_USER=loot_user
DB_HOST=localhost
DB_NAME=loot_tracking
DB_PASSWORD=your_secure_database_password_here
DB_PORT=5432

# JWT Configuration
JWT_SECRET=your_256_bit_jwt_secret_here_minimum_32_bytes_random_data

# External Services
OPENAI_API_KEY=your_openai_api_key_here

# Server Configuration
PORT=5000
NODE_ENV=development

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# Email Configuration (if using)
SMTP_HOST=your_smtp_host
SMTP_PORT=587
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password

# Security Notes:
# - JWT_SECRET must be at least 32 bytes (256 bits) of cryptographically secure random data
# - Use environment-specific values for each deployment
# - Never commit the actual .env file to version control
# - Consider using a secrets management service for production
# - Rotate secrets regularly
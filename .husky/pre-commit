echo "🔍 Running pre-commit checks..."

# Run linting and formatting on frontend
echo "🧹 Running lint-staged..."
cd frontend && npx lint-staged || { echo "❌ Lint-staged failed"; exit 1; }

echo "✅ Pre-commit checks passed!"
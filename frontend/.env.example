# Frontend Environment Variables
# Copy this file to .env and fill in your actual values

# API Configuration
REACT_APP_API_URL=/api

# Build Configuration
GENERATE_SOURCEMAP=true

# Development Configuration
BROWSER=none
FAST_REFRESH=true

# Security Notes:
# - Only variables prefixed with REACT_APP_ are accessible in the browser
# - Never include sensitive data like API keys in frontend environment variables
# - All frontend environment variables are public in the built application
# - Use backend proxy for sensitive API calls